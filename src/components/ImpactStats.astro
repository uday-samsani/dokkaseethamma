---
const stats = [
  { value: '50,000+', label: 'Meals Served', icon: '🍲' },
  { value: '10,000+', label: 'People Fed', icon: '👥' },
  { value: '150+', label: 'Events Conducted', icon: '📅' },
  { value: '3+', label: 'Years of Service', icon: '❤️' },
];
---

<section class="py-16 lg:py-24 bg-gradient-to-b from-cream-50 to-golden-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <span class="inline-block px-4 py-1.5 rounded-full bg-golden-100 text-golden-700 text-sm font-medium mb-4">
        Our Impact
      </span>
      <h2 class="text-3xl lg:text-4xl font-bold text-earth-900 mb-4">
        Making a <span class="text-golden-600">Difference</span>
      </h2>
      <p class="text-earth-600 max-w-2xl mx-auto">
        Every meal served, every smile shared, every life touched - together we create ripples of change.
      </p>
    </div>

    <div class="grid grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
      {stats.map((stat, index) => (
        <div 
          class="group relative bg-white rounded-2xl p-6 lg:p-8 shadow-sm hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1 text-center"
          style={`animation-delay: ${index * 100}ms`}
        >
          <!-- Decorative gradient border -->
          <div class="absolute inset-0 rounded-2xl bg-gradient-to-br from-golden-400/20 to-golden-600/20 opacity-0 group-hover:opacity-100 transition-opacity"></div>
          
          <div class="relative">
            <span class="text-4xl lg:text-5xl mb-4 block">{stat.icon}</span>
            <div class="text-3xl lg:text-4xl font-bold text-golden-600 mb-2">
              {stat.value}
            </div>
            <div class="text-earth-600 font-medium">
              {stat.label}
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
