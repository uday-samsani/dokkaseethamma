---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`rounded-xl border border-cream-200 bg-white text-earth-800 shadow-sm ${className}`}>
  <slot />
</div>
